<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login Page</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f0f2f5;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      overflow: hidden;
    }

    .login-container {
      background-color: #fff;
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
      width: 350px;
      animation: slideDown 1s ease-out;
    }

    h2 {
      text-align: center;
      color: #333;
      margin-bottom: 20px;
    }

    input[type="email"],
    input[type="password"] {
      width: 100%;
      padding: 12px;
      margin: 10px 0;
      border: 1px solid #ddd;
      border-radius: 5px;
      font-size: 14px;
      transition: 0.3s;
    }

    input[type="email"]:hover,
    input[type="password"]:hover {
      border-color: #007bff;
      box-shadow: 0 0 5px rgba(0, 123, 255, 0.3);
    }

    input[type="submit"] {
      width: 100%;
      padding: 12px;
      background-color: #007bff;
      color: #fff;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      font-size: 16px;
      transition: background-color 0.3s ease;
    }

    input[type="submit"]:hover {
      background-color: #0056b3;
    }

    .signup-link {
      text-align: center;
      margin-top: 15px;
      font-size: 14px;
    }

    .signup-link a {
      color: #28a745;
      text-decoration: none;
      transition: color 0.3s;
    }

    .signup-link a:hover {
      color: #218838;
      text-decoration: underline;
    }

    @keyframes slideDown {
      0% {
        transform: translateY(-50px);
        opacity: 0;
      }
      100% {
        transform: translateY(0);
        opacity: 1;
      }
    }

    /* Add floating animation for the input fields */
    input[type="email"],
    input[type="password"] {
      position: relative;
      z-index: 1;
    }

    input[type="email"]:focus,
    input[type="password"]:focus {
      outline: none;
      border-color: #007bff;
      box-shadow: 0 0 8px rgba(0, 123, 255, 0.4);
    }

    /* Add subtle transition on the form container */
    .login-container:hover {
      box-shadow: 0 15px 40px rgba(0, 0, 0, 0.2);
    }

    .invalid {
      color: red;
      text-align: center;
    }

    /* Responsive styles for mobile screens */
    @media (max-width: 480px) {
      /* Reduce login container width for small screens */
      .login-container {
        width: 90%; /* Adjusted width for mobile */
        padding: 20px; /* Adjusted padding for better fit */
      }

      h2 {
        font-size: 1.5rem; /* Adjusted font size for mobile */
      }

      input[type="email"],
      input[type="password"] {
        font-size: 1rem; /* Adjusted input font size for better readability */
      }

      input[type="submit"] {
        font-size: 1rem; /* Adjusted button font size */
      }

      .signup-link {
        font-size: 0.9rem; /* Adjusted font size for the signup link */
      }
    }
  </style>
</head>
<body>

  <div class="login-container">
    <h2>Login</h2>
    <form action="/user/login" method="post">
      <% if (locals.message) { %>
        <div class="invalid"><%= message %></div>
        <input type="email" name="email" placeholder="Email" required>
        <input type="password" name="password" placeholder="Password" required>
      <% } else { %>
        <% if (locals.error) { %>
          <div class="invalid"><%= error %></div>
          <input type="email" name="email" placeholder="Email" required>
          <input type="password" name="password" placeholder="Password" required>
        <% } else { %>
          <input type="email" name="email" placeholder="Email" required>
          <input type="password" name="password" placeholder="Password" required>
        <% } %>
      <% } %>
      <input type="submit" value="Login">
    </form>
    <div class="signup-link">
      Don't have an account? <a href="/signup">Signup here</a>
    </div>
  </div>

</body>
</html>
